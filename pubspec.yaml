name: green_urban_grow
description: Urban gardening app for small spaces
version: 1.0.0
environment:
  sdk: ">=2.17.0 <3.0.0"
  
dependencies:
  flutter:
    sdk: flutter
  url_launcher: ^6.0.20   # Stable with Dart 2.17
  http: ^0.13.5           # For News Feed API

flutter:
  uses-material-design: true
