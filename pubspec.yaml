name: green_urban_grow
description: Urban gardening app for small spaces
version: 1.0.0
environment:
  sdk: ">=2.17.0 <3.0.0"
  
dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.5
  url_launcher: ^6.0.20

flutter:
  uses-material-design: true
